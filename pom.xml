<?xml version="1.0" encoding="UTF-8"?>
<!--
    =====================================================================
    pom.xml — Descriptor Maven del proyecto SGIPEC
    Sistema de Gestión de Ingresos de Proveedores en Establecimiento
    Carcelario
    Autora : Heydi Estupiñan Estupiñán
    Versión: 1.0.0
    =====================================================================
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                            http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- ── Coordenadas del proyecto ── -->
    <groupId>co.sgipec</groupId>
    <artifactId>sgipec</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>SGIPEC</name>
    <description>
        Sistema de Gestión de Ingresos de Proveedores en Establecimiento Carcelario.
        Desarrollado con Java 17, JDBC, MySQL 8 y Java Swing.
    </description>

    <properties>
        <!-- Compilador Java 17 LTS -->
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Clase principal para el JAR ejecutable -->
        <main.class>co.sgipec.ui.VentanaPrincipal</main.class>
    </properties>

    <dependencies>

        <!-- MySQL Connector/J 8.3.0 — Driver JDBC oficial de MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>8.3.0</version>
        </dependency>

        <!-- JUnit 5 — Framework de pruebas unitarias -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.1</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!-- Plugin de compilación — Java 17 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <!-- Plugin Surefire — ejecución de pruebas JUnit 5 con mvn test -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
            </plugin>

            <!-- Plugin Assembly — genera el JAR ejecutable con todas las
                dependencias incluidas (fat JAR) mediante: mvn package -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${main.class}</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                    <finalName>sgipec-${project.version}</finalName>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

</project>
