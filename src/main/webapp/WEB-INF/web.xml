<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>SGIPEC - Sistema de Gestión de Ingresos de Proveedores</display-name>
    <description>
        Módulo web del SGIPEC desarrollado con Servlets y JSP.
        Proyecto: Análisis y Desarrollo de Software - SENA ADSO
        Autora: Heydi Estupiñán Estupiñán
        Ficha: 3186650
    </description>

    <!-- ── Servlet: Registro de Proveedores (GET y POST) ──────────────── -->
    <servlet>
        <servlet-name>RegistroProveedorServlet</servlet-name>
        <servlet-class>com.sgipec.servlet.RegistroProveedorServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>RegistroProveedorServlet</servlet-name>
        <url-pattern>/RegistroProveedor</url-pattern>
    </servlet-mapping>

    <!-- ── Servlet: Búsqueda de Proveedores (GET) ─────────────────────── -->
    <servlet>
        <servlet-name>BuscarProveedorServlet</servlet-name>
        <servlet-class>com.sgipec.servlet.BuscarProveedorServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>BuscarProveedorServlet</servlet-name>
        <url-pattern>/BuscarProveedor</url-pattern>
    </servlet-mapping>

    <!-- ── Página de bienvenida ──────────────────────────────────────── -->
    <welcome-file-list>
        <welcome-file>RegistroProveedor</welcome-file>
    </welcome-file-list>

    <!-- ── Configuración de sesión: 30 minutos ───────────────────────── -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- ── Codificación UTF-8 ────────────────────────────────────────── -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.apache.catalina.filters.SetCharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>
